version: '1.0'
steps:
  myComposition:
    type: composition
    title: My Composition from file
    composition: docker-compose.yml
    composition_candidates:
      test_service:
        image: tutum/curl
        command: curl http://web
